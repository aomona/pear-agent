# Server-only Gemini API key (Voice Live + free-text structure + plan improve).
# Free-text structure uses gemini-3.1-flash-lite-preview with thinking off (low latency).
# Optional: without it, Continuation / Plan / Replan still work; Voice + free-text fail.
GEMINI_API_KEY=
