# Server-only Gemini API key (Voice Live + free-text structure + plan improve).
# Free-text structure uses gemini-3.1-flash-lite with thinking off (low latency).
# Optional for the basic structured-input flow. Without it, Voice, free-text,
# plan improve, Gemini ordering, and AI replanning are unavailable.
GEMINI_API_KEY=
